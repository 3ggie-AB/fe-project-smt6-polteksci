# @workspace/network-monitor

To install dependencies:

```bash
npm install
```

To run the development server:

```bash
npm run dev
```

To build and preview the production output:

```bash
npm run build
npm run serve
```

Demo login:

```text
email: admin@example.com
password: password
```
